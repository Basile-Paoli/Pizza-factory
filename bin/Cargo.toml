[package]
name = "bin"
version = "0.1.0"
edition = "2024"

[dependencies]
agent = { path = "../agent" }
client = { path = "../client" }
parser = { path = "../parser" }
shared = { path = "../shared" }
clap = { version = "4", features = ["derive"] }
